<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:reg="http://www.dangdang.com/schema/ddframe/reg"
       xmlns:job="http://www.dangdang.com/schema/ddframe/job"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.dangdang.com/schema/ddframe/reg
                        http://www.dangdang.com/schema/ddframe/reg/reg.xsd
                        http://www.dangdang.com/schema/ddframe/job
                        http://www.dangdang.com/schema/ddframe/job/job.xsd">
    <!--配置作业注册中心 -->
    <reg:zookeeper id="studyJobCenter" server-lists="${zookeeper.list}"
                   namespace="study-job"
                   base-sleep-time-milliseconds="1000"
                   max-sleep-time-milliseconds="3000"
                   max-retries="3"/>

    <!--数据源-->
    <bean id="dataSourceForDd" class="com.alibaba.druid.pool.DruidDataSource" init-method="init" destroy-method="close">
        <property name="url" value="${job.jdbc.url}" />
        <property name="username" value="${job.jdbc.username}" />
        <property name="password" value="${job.jdbc.password}" />
    </bean>

    <!-- 配置作业msgPushJob-->
    <job:simple id="testJob" class="com.mouse.study.service.job.TestJob"
                registry-center-ref="studyJobCenter"
                cron="${test.cron}" sharding-total-count="1" sharding-item-parameters="0=A"
                overwrite="true" description="测试定时任务" event-trace-rdb-data-source="dataSourceForDd" />

</beans>