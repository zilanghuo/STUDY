<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mouse.study.dao.HealthDao">
    <!-- 健康检查 -->
    <select id="checkHealth" parameterType="java.util.Map" resultType="java.lang.Integer" statementType="STATEMENT">
        select count(1) countSize FROM ${tableName}
    </select>

</mapper>