<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zdmoney.message.log.dao.LogDataStatDayDAO">
    <resultMap type="com.zdmoney.message.log.model.LogDataStatDay" id="getMap">
    </resultMap>

    <insert id="insertLogStatDay" parameterType="java.util.List">
        INSERT INTO T_LOG_STAT_DAY (SYSTEM_NAME,METHOD_NAME,AVG_SPEND_TIME,AMOUNT,STAT_DATE,CREATE_TIME, MODIFY_TIME, OPERATOR)
        VALUES
        <foreach collection="list" item="log" index="index" separator=",">
            (#{log.systemName}, #{log.methodName},#{log.avgSpendTime},#{log.amount},#{log.statDate},#{log.createTime},
            #{log.modifyTime}, #{log.operator})
        </foreach>
    </insert>

</mapper>

