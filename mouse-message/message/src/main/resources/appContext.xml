<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!--读取配置文件-product-->
    <context:property-placeholder location="file:${global.config.path}/message.properties"/>

    <!--读取配置文件-local-->
    <!--<context:property-placeholder location="classpath:config/message.properties"/>-->

    <!--通过注解自动获取配置值-->
    <context:annotation-config/>

    <!--支持AOP-->
    <aop:aspectj-autoproxy/>

    <!--扫描package-->
    <context:component-scan base-package="com.zdmoney.message">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <context:component-scan base-package="com.zdmoney.zdqd"></context:component-scan>
    <bean id="SpringContextHelper" class="com.zdmoney.message.utils.SpringContextHelper"/>

    <bean id="mpAccount" class="io.github.elkan1788.mpsdk4j.vo.MPAccount">
        <property name="mpId" value="${msg.wechat.mpId}"/>
        <property name="appId" value="${msg.wechat.appId}"/>
        <property name="appSecret" value="${msg.wechat.appSecret}"/>
        <property name="token" value="${msg.wechat.token}"/>
        <property name="AESKey" value="${msg.wechat.aesKey}"/>
    </bean>

    <bean id="wechatAPI" class="io.github.elkan1788.mpsdk4j.api.WechatAPIImpl" factory-method="create">
        <constructor-arg ref="mpAccount"/>
    </bean>

    <!--mybatis-->
    <import resource="spring/appContext_mybatis.xml"/>
    <!--定时任务-->
    <import resource="spring/appContext_task.xml"/>
    <!--消息队列-->
    <import resource="spring/appContext_mq.xml"/>
    <!--redis-->
    <import resource="spring/appContext_redis.xml"/>

</beans>